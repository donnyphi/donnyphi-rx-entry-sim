"""Test package for the Rx Entry Simulator.

This file's presence makes ``tests`` an importable package so that
``python -m unittest discover -s tests`` (and ``run_checks.py``) can find
and load the test modules. It intentionally contains no code.
"""
